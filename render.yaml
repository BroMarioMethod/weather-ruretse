services:
  - type: web
    name: ruretse-weather
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python startup.py && python -m gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.3"